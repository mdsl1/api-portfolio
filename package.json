{
    "name": "api-portfolio",
    "version": "1.0.0",
    "scripts": {
        "start:v1": "npm --prefix v1 run start",

        "dev:v2": "npm --prefix v2 run start:dev",
        "build:v2": "npm --prefix v2 install && npm --prefix v2 run build"
    }
}